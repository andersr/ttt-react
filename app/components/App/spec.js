describe('<App />', () => {
  // let selectedPlayer
  // let testGame

  beforeEach(() => {
    jest.resetModules()
  })

  it('displays <SelectPlayerMenu /> if humanPlayer is null')
  // , () => {
    // ensure default state
    // call dispatch
    // mount(?) <App />
    // check if <SelectPlayerMenu /> is found
  // })
})
