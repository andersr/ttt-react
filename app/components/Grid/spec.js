describe('<GameSquare />', () => {
  it('has a default empty content')
  it('requires a content component')
  it('hides by default when the backdrop is clicked')
  it('hides when a hideModal callback is called (eg based on an event in the content component)')
  it('does not hide when the backdrop is clicked if a static prop is provided')
})
