const initialState = {
  humanPlayer: null
}



const tttApp = (state = initialState, action) => {
  // switch (action.type) {
  //   case 'SELECT_PLAYER':
  //     return [
  //       ...state,
  //       todo(undefined, action)
  //     ]
  //   case 'TOGGLE_TODO':
  //     return state.map(t =>
  //       todo(t, action)
  //     )
    // default:
      return state
  // }
}

export default tttApp
